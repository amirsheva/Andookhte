# اندوخته — بریف معرفی محصول برای طراح UI/UX

> این سند برای این نوشته شده که یک طراح حرفه‌ای، بدون دیدن طرح فعلی و بدون تعصب به اجرای بصری موجود، بفهمد این محصول چیست، چه کسانی از آن استفاده می‌کنند، چرا ساخته شده، و چه بخش‌هایی دارد. عمداً دربارهٔ رنگ، فونت، چیدمان یا سبک بصری فعلی چیزی گفته نشده — می‌خواهیم ایده‌های تازه ببینیم، نه بازطراحی همان چیزی که هست.

---

## ۱. اندوخته چیست؟

**اندوخته** یک اپلیکیشن وب (PWA، قابل نصب روی موبایل) برای مدیریت مالی شخصی و خانوادگی است. آدرس‌محور فارسی‌زبانان ایران است — نه یک ترجمهٔ یک اپ خارجی.

جمله‌ی یک‌خطی: *«همه‌ی دارایی، خرج‌وخرج، بدهی و طلبت را در یک‌جا، به زبان و تقویم خودت، ببین.»*

---

## ۲. برای چه کسی می‌سازیمش؟

- کسی که چند نوع دارایی دارد، نه فقط یک حساب بانکی: پول نقد، حساب بانکی، طلا، ارز (دلار/یورو و…)، رمزارز، صندوق پس‌انداز مشترک با دوستان/خانواده.
- کسی که در اقتصاد ایران زندگی می‌کند: نوسان قیمت ارز و طلا بخشی از واقعیت روزمره‌اش است، پس می‌خواهد بداند طلا و ارزش امروز چقدر می‌ارزد، نه فقط چند گرم دارد.
- خانواده یا شریک‌های مالی که می‌خواهند یک «فضای کاری» مشترک داشته باشند — با نقش‌های متفاوت (فقط دیدن، ثبت‌کردن، مدیریت‌کردن).
- کسی که وام یا قرض دارد (به بانک، یا به شخص) و می‌خواهد اقساطش را روی تقویم شمسی دنبال کند، نه اکسل جداگانه.
- کاربر پیشرفته‌تری که می‌خواهد داده‌هایش را دسته‌جمعی وارد/خارج کند (اکسل)، یا حتی بدون باز کردن اپ، فقط با یک حرکت روی گوشی (شورتکات آیفون) یک هزینه ثبت کند.

---

## ۳. چشم‌انداز و نگاه ما

1. **موجودی، واقعیت است نه عدد قابل ویرایش.** موجودی هر حساب همیشه از جمع تراکنش‌هایش به دست می‌آید. کاربر نمی‌تواند مستقیم عدد موجودی را دستکاری کند — اگر عددی اشتباه است، باید یک تراکنش اصلاحی ثبت شود. این یک اصل غیرقابل‌مذاکره در طراحی تجربه است: هر جایی که موجودی نمایش داده می‌شود، باید همیشه «قابل ردیابی به یک تراکنش واقعی» باشد.
2. **تنوع دارایی، تظاهر نیست — واقعیت اقتصادی است.** طلا و ارز را نباید مثل یک ردیف اضافه در لیست حساب‌ها نگاه کرد؛ این‌ها روش اصلی پس‌انداز خیلی از کاربران ایرانی‌اند. باید حس شود که اپ این تفاوت را واقعاً «می‌فهمد» (مثلاً برای طلا: وزن، عیار، نوع کالا؛ برای ارز/رمزارز: نرخ روز و معادل ریالی).
3. **حریم خصوصی مالی مهم است.** آدم‌ها گاهی گوشی‌شان را جلوی کس دیگری باز می‌کنند. باید همیشه راهی برای «مخفی کردن سریع عدد» روی صفحه باشد، بدون اینکه از اپ خارج شوند.
4. **فارسی و شمسی، زبان دوم نیست — زبان اول است.** تاریخ‌ها همیشه شمسی، جهت رابط کاربری راست‌به‌چپ، اعداد فارسی. این‌ها «قابلیت اضافه» نیستند، بلکه فرض پایه‌ی طراحی‌اند.
5. **اعتماد از طریق شفافیت.** وقتی یک عملیات (حذف دسته‌جمعی، وارد کردن اکسل، حذف یک تراکنش) روی پول واقعی اثر می‌گذارد، کاربر باید دقیقاً بداند چه اتفاقی افتاد — از جمله وقتی بخشی از یک عملیات دسته‌جمعی موفق و بخشی ناموفق بوده.
6. **اپ باید فراتر از خودش هم کار کند.** خیلی از کاربران دوست دارند بدون باز کردن اپ، سریع یک هزینه ثبت کنند (مثلاً با یک حرکت پشت گوشی آیفون) یا داده‌هایشان را در اکسل/گوگل‌شیت هم داشته باشند. اپ نباید یک «جزیره‌ی بسته» باشد.
7. **حس کیفیت، بدون پیچیدگی اضافه.** کاربر نباید احساس کند وارد یک نرم‌افزار حسابداری شرکتی شده؛ باید ساده، سریع، و کمی «شیک» باشد — چیزی که آدم‌ها دوست دارند هر روز بازش کنند، نه فقط وقتی مجبورند.

---

## ۴. بخش‌های اصلی برنامه

### ۴.۱ ورود و ثبت‌نام
ورود با ایمیل/رمز عبور یا با کد پیامکی یک‌بارمصرف. بازیابی رمز عبور از طریق ایمیل. کاربر تازه‌وارد خودکار یک «فضای کاری شخصی» می‌گیرد.

### ۴.۲ فضای کاری (Workspace)
واحد اصلی جداسازی داده. هر کاربر می‌تواند چند فضای کاری داشته باشد (مثلاً «شخصی من» و «مشترک با همسر»)، بین‌شان جابه‌جا شود، و در هرکدام یک نقش داشته باشد: بیننده، حسابدار (می‌تواند ثبت کند)، مدیر (می‌تواند عضو اضافه/حذف کند)، مالک. افزودن عضو با ایمیل یا شماره موبایل. نام فضای کاری قابل تغییر است.

### ۴.۳ داشبورد
نمای کلی لحظه‌ای: مجموع دارایی (قابل مخفی‌کردن)، درآمد/هزینه/ماندهٔ دورهٔ اخیر با درصد تغییر نسبت به دورهٔ قبل، نمودار روند درآمد/هزینه، نمودار دایره‌ای هزینه بر اساس دسته، کارت‌های بصری حساب‌ها (قابل کلیک برای جزئیات/ویرایش)، و آخرین تراکنش‌ها.

### ۴.۴ حساب‌ها
شش نوع حساب، هرکدام با فیلدهای مخصوص خودش:
- **بانکی** — شماره کارت (با تشخیص خودکار بانک از روی شش رقم اول)، شماره شبا، نام بانک.
- **نقدی** — ساده، بدون فیلد اضافه.
- **صندوق پس‌انداز** — یادداشت آزاد (مثلاً «صندوق دوستان»).
- **طلا** — وزن (گرم)، عیار، نوع کالا (سکه/انگشتر/گردنبند/گوشواره/طلای خام/…)، و نرخ روز هر گرم (دستی) که ارزش تخمینی امروز را جدا از موجودی واقعی نشان می‌دهد.
- **ارز** — نوع ارز (دلار، یورو، پوند، درهم، لیر)، نرخ روز (دستی) و معادل ریالی محاسبه‌شده.
- **رمزارز** — نماد (BTC، ETH، USDT، …)، نرخ روز (دستی) و معادل ریالی.

هر حساب: موجودی قابل مخفی‌کردن، امکان «اصلاح موجودی» (که در پشت صحنه یک تراکنش اصلاحی می‌سازد، نه ویرایش مستقیم عدد)، و امکان ویرایش/حذف (حذف فقط اگر تراکنشی به آن وصل نباشد).

### ۴.۵ تراکنش‌ها
ثبت هزینه/درآمد/انتقال بین حساب‌ها، با دسته‌بندی و توضیح و تاریخ. جست‌وجو و فیلتر (نوع، دسته، حساب). گروه‌بندی بر اساس روز با جمع روزانه. صفحه‌بندی برای تاریخچهٔ طولانی.

قابلیت‌های دسته‌جمعی:
- **انتخاب چندتایی و حذف گروهی** با گزارش دقیق اینکه کدام‌ها موفق و کدام‌ها (و چرا) ناموفق بودند.
- **خروجی اکسل** از همهٔ تراکنش‌ها.
- **ورودی اکسل** برای ثبت دسته‌جمعی — هر ردیف جدا اعتبارسنجی می‌شود، یک ردیف خراب بقیه را متوقف نمی‌کند.

### ۴.۶ بدهی و طلب
پیگیری وام‌هایی که گرفته‌ایم یا قرض‌هایی که داده‌ایم. سه حالت: یک‌باره، اقساط ثابت، ماهیانهٔ بازگشتی (که بعداً هم قابل تمدید است). تقویم شمسی با نشانگر روزهای دارای قسط. ثبت پرداخت هر قسط (که یک تراکنش واقعی هم می‌سازد). امکانات ویژه:
- وارد کردن وامی که از قبل شروع شده و چند قسطش پرداخت شده (بدون نیاز به تراکنش واقعی برای گذشته).
- اصلاح تاریخ شروع کل زمان‌بندی (همهٔ اقساط با هم جابه‌جا می‌شوند).
- یادآوری خودکار سررسید از طریق ایمیل.

### ۴.۷ تحلیل
نرخ پس‌انداز، میانگین هزینهٔ روزانه، نمودار جریان نقدی در بازه‌های مختلف (۱/۳/۶ ماه)، تفکیک هزینه یا درآمد بر اساس دسته، و مقایسهٔ ماه‌به‌ماه (شش ماه اخیر شمسی).

### ۴.۸ پروفایل و تنظیمات
اطلاعات هویتی (نام، ایمیل، موبایل)، تأیید ایمیل/موبایل، تغییر رمز عبور، تغییر نام فضای کاری، و مدیریت اعضای فضای کاری (برای نقش‌های مدیر/مالک).

### ۴.۹ اتوماسیون و یکپارچگی
- **کلید دسترسی شخصی (API Key):** کاربر می‌تواند کلید بسازد و با آن، بدون باز کردن اپ، از ابزارهای بیرونی (مثل شورتکات آیفون که با دو ضربه پشت گوشی فعال می‌شود) مستقیم یک تراکنش ثبت کند.
- **ورود/خروج اکسل** (توضیح در بخش تراکنش‌ها) — قدم اول برای یکپارچگی با ابزارهای بیرونی؛ اتصال زنده به گوگل‌شیت در نقشهٔ راه بعدی است.

---

## ۵. الزامات و محدودیت‌های ثابت (برای هر طرحی که پیشنهاد شود)

- **راست‌به‌چپ کامل و فارسی.** جهت، اعداد، تاریخ — همه فارسی/شمسی.
- **موبایل، اولویت اول.** اپ باید PWA و قابل‌نصب باشد؛ اکثر کاربران از موبایل استفاده می‌کنند، نسخهٔ دسکتاپ باید همان کیفیت را داشته باشد ولی طراحی نباید با فرض دسکتاپ شروع شود.
- **حالت روشن و تاریک** هر دو باید به یک اندازه با کیفیت باشند.
- **چندکاربره با سطح دسترسی.** بعضی بخش‌ها (مثلاً حذف، مدیریت اعضا) فقط برای نقش‌های بالاتر قابل مشاهده/انجام‌اند.
- **داده‌های حساس مالی.** هر جا عدد پول نشان داده می‌شود، باید امکان مخفی‌کردن سریع وجود داشته باشد.

---

## ۶. از طراح چه می‌خواهیم

- نگاه تازه به معماری اطلاعات و تجربهٔ بصری — لازم نیست ادامهٔ چیزی باشد که الان وجود دارد.
- تمرکز روی این‌که «تنوع دارایی» (طلا، ارز، رمزارز در کنار بانکی و نقدی) در نگاه اول قابل تشخیص باشد، بدون شلوغی.
- پیشنهاد برای این‌که داشبورد و کارت‌های حساب چطور می‌توانند هم زیبا باشند هم «به یک نگاه» قابل‌فهم.
- ایده برای نمایش بهتر داده‌های عددی مالی (نمودارها، مقایسه‌ها) به شکلی که برای کاربر غیرحرفه‌ای هم قابل‌فهم باشد.
- در نظر گرفتن این‌که این یک ابزار روزمره است، نه یک‌بار مصرف — باید چیزی باشد که آدم‌ها با میل باز کنند.

با تشکر — برای هر بخش که نیاز به جزئیات بیشتری داشت (مثلاً دقیقاً چه فیلدهایی در فرم ثبت تراکنش هست)، بگویید تا اضافه کنیم.
