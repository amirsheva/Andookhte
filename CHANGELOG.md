# تاریخچهٔ نسخه‌ها

تا امروز پروژه بدون شمارهٔ نسخهٔ رسمی جلو رفته بود (نه در `package.json`، نه با تگ گیت). این فایل، ۱۶ کامیت موجود را بر اساس اندازهٔ تغییرشان به نسخه‌های معنادار (Semantic Versioning) تقسیم می‌کند تا از این به بعد هر تغییر عدد مشخصی داشته باشد.

قاعده از این به بعد: **بخش اول** = تغییر بزرگ/شکنندهٔ ساختار، **بخش دوم** = قابلیت تازه، **بخش سوم** = رفع باگ یا اصلاح جزئی.

---

## [نسخهٔ فعلی] 1.5.1 — رفع باگ واحد پول در تراکنش‌ها

- **رفع باگ:** فرم ثبت تراکنش، واحد مبلغ را همیشه از اولین حساب کاربر می‌گرفت، نه از حساب واقعاً انتخاب‌شده — روی حساب دلاری/رمزارزی، عدد وارد‌شده به‌گمان ریال، مستقیم و بدون تبدیل از موجودی همان واحد خارجی کم/زیاد می‌شد.
- **رفع باگ:** جمع درآمد/هزینه در داشبورد و صفحهٔ تحلیل، تراکنش‌های ریالی و ارزی/رمزارزی را با هم جمع می‌زد.
- انتقال مستقیم بین دو حساب با واحد متفاوت (مثلاً ریالی و دلاری) اکنون مسدود می‌شود.
- نمایش تراکنش‌ها اکنون واحد صحیح (ریال/دلار/نماد رمزارز) را کنار مبلغ نشان می‌دهد.

## 1.5.0 — کلید API، اکسل، حذف دسته‌جمعی، نرخ طلا

- زیرساخت کلید دسترسی شخصی (API Key) برای اتوماسیون‌هایی مثل شورتکات آیفون.
- خروجی/ورودی اکسل برای تراکنش‌ها، با گزارش ردیف‌به‌ردیف.
- حذف دسته‌جمعی تراکنش‌ها.
- نرخ روز برای طلا (وزن × نرخ = ارزش تخمینی، جدا از موجودی واقعی).
- امکان تغییر نام فضای کاری.

## 1.4.0 — بازطراحی انواع حساب

- تفکیک حساب طلا و ارز به دو نوع مستقل، با فیلدهای اختصاصی (وزن/عیار طلا، نماد رمزارز).
- نرخ دستی برای ارز/رمزارز و محاسبهٔ معادل ریالی.
- اصلاح زمان‌بندی بدهی (جابه‌جایی تاریخ شروع همهٔ اقساط) و امکان وارد کردن وامی که چند قسطش از قبل پرداخت شده.

## 1.3.1 — پرداخت‌های ظاهری

چهار دور رفع باگ‌های رابط کاربری از بازبینی اسکرین‌شات‌ها: گیر کردن منوی عملیات زیر نوار پایین، جای نادرست دکمهٔ شناور، بسته‌شدن ناخواستهٔ مودال با دکمهٔ نمایش شمارهٔ کارت، و چند اصلاح ظاهری دیگر.

## 1.3.0 — بدهی و طلب

مدل کامل پیگیری وام/قرض: بک‌اند (موجودیت‌ها، API، یادآوری ایمیلی) و فرانت‌اند (تقویم شمسی، فرم‌ها، ثبت پرداخت).

## 1.2.2 — اصلاح موجودی

امکان اصلاح موجودی حساب (با ثبت خودکار تراکنش اصلاحی)، قفل عرض صفحه هنگام باز بودن مودال، نوتیفیکیشن (toast) بعد از ذخیره.

## 1.2.1 — بازطراحی نوار بالا

رفع سرریز افقی نوار بالا در عرض‌های زیر ۵۶۰ پیکسل.

## 1.2.0 — ایمیل واقعی

ارسال کد یک‌بارمصرف (بازیابی رمز، تأیید ایمیل) از طریق SMTP واقعی، نه فقط لاگ.

## 1.1.0 — پرداخت‌های اولیهٔ تجربهٔ کاربری

دکمهٔ نمایش/مخفی‌کردن رمز عبور، غیرفعال‌کردن zoom با دو انگشت، واضح‌تر شدن مسیر ثبت‌نام.

## 1.0.0 — نسخهٔ پایه

حذف کامل ماژول انبار (که با نیاز واقعی محصول — مدیریت مالی شخصی — همخوانی نداشت) و استقرار اولیه روی سرور با PostgreSQL، Docker، و nginx.

---

*شمارهٔ نسخه در `andookhte-web/package.json` و `src/Directory.Build.props` نگهداری می‌شود. هر دو با هم به‌روزرسانی شوند.*
